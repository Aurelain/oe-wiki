/**
 *
 */
const assume = (condition, message, ...args) => {
    if (!condition) {
        const error = new Error(message);
        error.extra = args;
        throw error;
    }
};

// =====================================================================================================================
//  E X P O R T
// =====================================================================================================================
export default assume;
